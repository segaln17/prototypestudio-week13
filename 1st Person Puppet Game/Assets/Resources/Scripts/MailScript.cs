using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MailScript : MonoBehaviour
{
    //if player enters a trigger, press E to send letter or something
    //changes arm to non letter arm
    //coroutine starts, puppet waits for letter to come
    //then letter appears
    //puppet can take letter --> destroy the gameobject, sets arm to letter arm, adds to inventory
    //take it to desk, read at desk
    //letter writing is animation that is triggered by bools or something
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
